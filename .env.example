# Copy to .env.local and fill in. If left unset, the app falls back to the
# Supabase project baked into src/supabaseClient.js (the shared "somewhere" project),
# or runs in DEMO mode when that project hasn't been set up yet.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
